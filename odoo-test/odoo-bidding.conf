[options]
; BIDDING sandbox — synthetic data only. Serves ONLY deveres_bidding from its
; own addons directory (includes sor_bidding, which must never reach the
; client-parity addons path). Tailnet-only by default — not funneled.
addons_path = /mnt/extra-addons,/usr/lib/python3/dist-packages/odoo/addons
data_dir = /var/lib/odoo
db_host = db
db_port = 5432
db_user = odoo
db_password = odoo_test_2026
dbfilter = ^deveres_bidding$
list_db = False
admin_passwd = CHANGE-ME-BEFORE-EXPOSING
proxy_mode = True
workers = 0
log_level = info
