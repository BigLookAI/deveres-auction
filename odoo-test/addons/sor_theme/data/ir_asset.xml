<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data noupdate="0">
    <!-- Navbar asset overrides removed (caused 500). post_init_hook deletes any existing records. Theme SCSS (style.scss) still applies. -->
  </data>
</odoo>

