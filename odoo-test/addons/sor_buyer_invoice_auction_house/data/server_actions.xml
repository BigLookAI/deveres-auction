<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Bulk-send server action available from the Buyer Invoices list -->
        <record id="action_server_bulk_send_buyer_invoice" model="ir.actions.server">
            <field name="name">Send to Buyers</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="binding_model_id" ref="account.model_account_move"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">action = records.action_bulk_send_sor_invoice()</field>
        </record>
    </data>
</odoo>
